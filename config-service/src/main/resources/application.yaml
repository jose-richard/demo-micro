server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: develop
          uri: https://github.com/jose-richard/demo-micro
          search-paths: config-data